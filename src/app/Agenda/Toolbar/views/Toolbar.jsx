/**
* @jsx React.DOM
*/
var React = require('react');
var Toolbar = React.createClass({
  render: function () {
    return (
      <div>
        Toolbar
      </div>
    );
  }
});
module.exports = Toolbar;